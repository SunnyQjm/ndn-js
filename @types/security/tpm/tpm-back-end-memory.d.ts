import {TpmBackEnd} from "./tpm-back-end";

export class TpmBackEndMemory extends TpmBackEnd {
    /**
     * TpmBackEndMemory extends TpmBackEnd to implement a TPM back-end using
     * in-memory storage.
     * @constructor
     */
    constructor();
}